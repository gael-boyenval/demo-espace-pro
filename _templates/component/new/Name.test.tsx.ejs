---
to: src/<%= dir %>/<%= h.capitalize(name) %>/__tests__/<%= h.capitalize(name) %>.test.tsx
---
/* eslint-disable @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-member-access,
@typescript-eslint/no-unsafe-call */
import React from 'react';
import { render } from '@testsutils';
import <%= h.capitalize(name) %> from '../<%= h.capitalize(name) %>';

const initialize = (options) => {
  const utils = render(<<%= h.capitalize(name) %> { ...props }/>)
  const doSomething = () => {}
  
  return {
    ...utils,
    doSomething
  }
}

describe('<%=name%>', () => {
  it('fails', () => {
    const { getByRole, unmount } = initialize();
    expect(false).toBe(true);
    unmount();
  })
})